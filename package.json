{
  "name": "csis-corporate-intelligence-openai-search",
  "version": "0.1.0",
  "private": true,
  "description": "CSIS dashboard and n8n workflow variant that uses OpenAI web search instead of Tavily.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js"
  }
}
